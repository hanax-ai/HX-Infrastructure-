# --- HX Gateway DB + Cache + Vector env ---
# Postgres (Citadel)
DATABASE_URL=postgresql+psycopg://citadel_admin:Major8859!@192.168.10.35:5432/citadel_ai

# Redis cache
REDIS_URL=redis://:Major8859!@192.168.10.35:6379/0

# Qdrant vector DB (optional, used by wrapper/RAG)
QDRANT_URL=http://192.168.10.30:6333
QDRANT_COLLECTION=hx_rag_default

# Master key expected by your YAML (general_settings.master_key uses ${MASTER_KEY})
MASTER_KEY=sk-hx-dev-1234
# Keep also the alternative name if you want (harmless)
LITELLM_MASTER_KEY=sk-hx-dev-1234
