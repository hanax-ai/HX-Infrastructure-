# HX-Infrastructure Environment Variables Template
# Copy this to .env and set your actual values
# The .env file is ignored by git to keep secrets safe

# Service Configuration
PORT=4010
BASE=http://127.0.0.1:4010

# Authentication (set these for testing with actual credentials)
# ADMIN_KEY=sk-hx-admin-your-actual-key
# AUTH=Bearer sk-hx-your-actual-token

# RAG Configuration
NS=docs:test

# Qdrant Configuration
QDRANT_URL=http://192.168.10.30:6333
COLL=hx_rag_default

# Gateway Directory
GATEWAY_DIR=/opt/HX-Infrastructure-/api-gateway/gateway
